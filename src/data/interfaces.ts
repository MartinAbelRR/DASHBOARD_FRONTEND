export interface Item {
    id: string;
    title: string;
    icon: any;
    path: string;
}

export interface Indicador {
    id: string;
    title: string;
    indicador: number;
    porcentaje: number;
    increment: number;
}